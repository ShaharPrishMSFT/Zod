# Block parsing limitations and supported syntax

context
--begin
Simple inline action.
Simple inline input.

Complex action with multiple lines and embedded logic:
If the user provides input, process it.

If some condition is met, perform the 'then' action.

Complex logic:
If some condition is met, do something.
--end
